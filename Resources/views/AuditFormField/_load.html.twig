{% block body %}
<tr class="wgaudit-field-row">
    <td>{{ field.title }}</td>
    <td>{{ field.description }}</td>
    <td>{{ field.weight }}</td>
    <td>
    {% if field.fatal == 1 %}
        <i class="icon-check"/>
    {% else %}
        <i class="icon-check-empty"/>
    {% endif %}
    </td>
    <td>
        <div class="btn-group">
            <a class="btn btn-mini wgaudit-field-view" data-field-id="{{ field.id }}" 
               href="{{ path( 'wgauditformfield_view', { 'id': field.id } ) }}">
                <i class="icon-eye-open"></i> View
            </a>
            <noscript>
                <a class="btn btn-mini"
                    href="{{ path( 'wgauditformfield_edit', { 'id': field.id } ) }}">
                    <i class="icon-wrench"></i> Edit
                </a>
                <a class="btn btn-mini" 
                   href="{{ path( 'wgauditformsection_remove', { 'id' : section.id ,'field_id': field.id } ) }}">
                    <i class="icon-minus"></i> Remove
                </a>                
            </noscript>
            <a class="btn btn-mini dropdown-toggle" data-toggle="dropdown" href="#">
                <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
                <li>
                    <a class="wgaudit-field-edit" data-field-id="{{ field.id }}"
                        href="{{ path( 'wgauditformfield_edit', { 'id': field.id } ) }}">
                        <i class="icon-wrench"></i> Edit
                    </a>
                </li>
                <li>
                    <a class="wgaudit-field-remove" data-field-id="{{ field.id }}"
                       href="{{ path( 'wgauditformsection_remove', { 'id' : section.id ,'field_id': field.id } ) }}">
                        <i class="icon-minus"></i> Remove
                    </a>
                </li>
            </ul>
        </div>
    </td>
</tr>
{% endblock %}
