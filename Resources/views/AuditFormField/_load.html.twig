{% block body %}
<tr class="cisco-audit-field-row">
    <td>{{ field.title }}</td>
    <td>{{ field.description }}</td>
    <td>{{ field.weight }}</td>
    <td>
    {% if field.fatal == 1 %}
        <i class="icon-check"/>
    {% else %}
        <i class="icon-check-empty"/>
    {% endif %}
    </td>
    <td>
        <div class="btn-group">
            <a class="btn btn-mini cisco-audit-field-view" data-field-id="{{ field.id }}" 
               href="{{ path( 'cisco_auditformfield_view', { 'id': field.id } ) }}">
                <i class="icon-eye-open"></i> View
            </a>
            <noscript>
                <a class="btn btn-mini"
                    href="{{ path( 'cisco_auditformfield_edit', { 'id': field.id } ) }}">
                    <i class="icon-wrench"></i> Edit
                </a>
                <a class="btn btn-mini" 
                   href="{{ path( 'cisco_auditformsection_remove', { 'id' : section.id ,'field_id': field.id } ) }}">
                    <i class="icon-minus"></i> Remove
                </a>                
            </noscript>
            <a class="btn btn-mini dropdown-toggle" data-toggle="dropdown" href="#">
                <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
                <li>
                    <a class="cisco-audit-field-edit" data-field-id="{{ field.id }}"
                        href="{{ path( 'cisco_auditformfield_edit', { 'id': field.id } ) }}">
                        <i class="icon-wrench"></i> Edit
                    </a>
                </li>
                <li>
                    <a class="cisco-audit-field-remove" data-field-id="{{ field.id }}"
                       href="{{ path( 'cisco_auditformsection_remove', { 'id' : section.id ,'field_id': field.id } ) }}">
                        <i class="icon-minus"></i> Remove
                    </a>
                </li>
            </ul>
        </div>
    </td>
</tr>
{% endblock %}
