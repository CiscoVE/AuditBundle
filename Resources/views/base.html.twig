<!DOCTYPE html>
<html>
  <head>
    {% block metas %}
      <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    {% endblock %}
    <title>{% block title %}Cisco Systems Audit Bundle{% endblock %}</title>
    {% block favicon %}{% endblock %}
    {% block stylesheets %}
        <link href='http://fonts.googleapis.com/css?family=Orienta' rel='stylesheet' type='text/css' />
        <link rel="stylesheet" href="http://code.jquery.com/ui/1.9.1/themes/base/jquery-ui.css" />
        <link href="http://netdna.bootstrapcdn.com/twitter-bootstrap/2.2.1/css/bootstrap-combined.min.css" rel="stylesheet" />
        <style type="text/css">
        body * {
            font-family: 'Orienta', sans-serif;
        }
        body {
            padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
        }
        .btn-round {
            height: 23px;
            width: 18px;
            border-radius: 50%;
        }
        .cisco-audit-section-row {
            background-color: #EEE;
        }
        </style>
    {% endblock %}
    {% block javascripts %}
            <script src="//ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
            <script src="//ajax.googleapis.com/ajax/libs/jqueryui/1.9.1/jquery-ui.min.js"></script>
            <script src="http://netdna.bootstrapcdn.com/twitter-bootstrap/2.2.1/js/bootstrap.min.js"></script>
            <script src="{{ asset( 'bundles/ciscosystemsaudit/js/ciscoaudit.js' ) }}" type="text/javascript"></script>
    {% endblock %}
  </head>
  <body>
    {% block subnavigation %}
    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">Audit Bundle</a>
          <div class="nav-collapse collapse">
            <ul class="nav">
              <li>
                  <a href="{{ path( 'cisco_audits' ) }}">view audit list</a>
              </li>
              <li>
                  <a href="{{ path( 'cisco_auditforms' ) }}">view form list</a>
              </li>
              <li>
                  <a href="{{ path( 'cisco_auditsections' ) }}">view section list</a>
              </li>
              <li>
                  <a href="{{ path( 'cisco_auditfields' ) }}">view field list</a>
              </li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>
    {% endblock %}
    {% block modal %}
    <div id="dialog-modal" title="Basic modal dialog" hidden="true">
        <div class="modal-content">Adding the modal overlay screen makes the dialog look more prominent because it dims out the page content.</div>
    </div>
    {% endblock %}
    {% block body %}{% endblock %}
  </body>
</html>





